# AI Interactive Classroom

## Overview
This repository contains the full-stack code for the AI Interactive Classroom application.

## Setup Instructions
1. Clone the repository.
2. Install dependencies using `npm install` in both the `client` and `server` directories.
3. Create a `.env` file with the necessary environment variables.
4. Run the application using `npm start` in both directories.

## Contributing
Please open issues for any features or bugs you find.